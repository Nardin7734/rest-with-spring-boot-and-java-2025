spring:
  application:
    name: rest-with-spring-boot-and-java-2025

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_springboot_2025?useTimezone=true&serverTimezone=UTC
    username: root
    password: interact*123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
logging:
  level:
    com.dn: DEBUG